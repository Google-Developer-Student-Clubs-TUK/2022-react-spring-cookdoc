spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver #driver
    url: jdbc:mysql://mysqldb:3307/cookdoc
    username: root # username
    password: root0707 # password

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace